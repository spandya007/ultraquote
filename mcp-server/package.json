{
  "name": "@smartprops/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Model Context Protocol server for SmartProps — drive the proposal workflow from an AI chat client (Claude Desktop, Cursor, etc.). Wraps the SmartProps /api/v1 REST API; authenticates with a SmartProps API key.",
  "type": "module",
  "bin": {
    "smartprops-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2"
  }
}
